# Data

This folder contains the dataset files that ship with the repository:

- `EMG-data.csv.zip` is the raw bundled dataset archive.
- `dataset_description.txt` is the original dataset description.

The extracted CSV is generated into `.cache_emg/` during preparation and is not committed.
