# Artifacts

This folder contains the public outputs from the project:

- `final_model.joblib` is the exported trained model.
- `final_metrics.json` is the final evaluation summary.
- `best_grouped_result.json` is the best grouped cross-validation run.
